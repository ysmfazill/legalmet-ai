# ---------------------------------------------------------------------------
# Root environment file — used by docker-compose for the PostgreSQL container.
# Copy to `.env` and adjust. NEVER commit the real `.env`.
# ---------------------------------------------------------------------------
POSTGRES_USER=legalmet
POSTGRES_PASSWORD=legalmet
POSTGRES_DB=legalmet
POSTGRES_PORT=5432
